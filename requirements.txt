# llama-cpp-python MUST be installed separately with Metal flags (see README).
# Running: pip install llama-cpp-python (without CMAKE_ARGS) will install a
# CPU-only build that silently disables GPU acceleration.
# Command: CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python
torch>=2.2.0
transformers>=4.40.0
huggingface-hub>=0.22.0
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.0.0
numpy>=1.26.0
tqdm>=4.66.0
accelerate>=0.24.0
pyyaml>=6.0
